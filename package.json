{
  "private": true,
  "name": "harlanzw-twitter",
  "scripts": {
    "build": "export NITRO_PRESET=vercel-edge && nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "npm:nuxt3@latest"
  },
  "engines": {
    "node": "14.x"
  },
  "dependencies": {
    "chrome-aws-lambda": "10.1.0",
    "puppeteer-core": "10.1.0",
    "nuxt-og-image": "^0.5.2"
  }
}
